export interface IssData {
    message: string
    timestamp: number
    position: {
        latitude: string
        longitude: string
    }
    height: number
    velocity: number
    period: number
}